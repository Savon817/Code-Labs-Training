class StaticPagesController < ApplicationController
    def about_us
        # render and redirect_to(also can't call render more than once)
        # render 'static_pages/about_us'

        
    end
end